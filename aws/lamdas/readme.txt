The lambda-package contains the zip file to be uploaded in the AWS for the 
lambda code. It contains the correct lambda code and the psycopg2-binary
and the manylinux2014 package. Everything is done and zipped into the
function.zip file. Just upload it.